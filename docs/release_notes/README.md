# Legacy release notes

Files already stored here document releases made by the former `/deploy`
workflow. New releases do not add files to this directory: `/release` stores
the notes in the annotated Git tag, and GitHub Actions uses that tag message as
the release body.
